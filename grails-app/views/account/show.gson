import finance.Account
import finance.Balance

model {
    Account account
}

//json tmpl.account(account)
json {
    id account.id
    name account.name
    balance account.balance, { Balance balance ->
        amount balance.amount
    }
}